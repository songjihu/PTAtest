/*#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {

	int x = 0, t = 0, a[4] = { 0,0,0,0 }, i = 0, j = 0;//a[]´æ¸÷Î»Êý
	scanf("%d", &x);
	t = x;
	for (i = 0; i < 3; i++) {
		a[i] = t % 10;
		t = t / 10;
	}
	for (i = 0; i < a[2]; i++) {
		printf("B");
	}
	for (i = 0; i < a[1]; i++) {
		printf("S");
	}
	for (i = 0; i < a[0]; i++) {
		printf("%d", i + 1);
	}

	return 0;
}
*/